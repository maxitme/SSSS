export interface Customer {
    id: string; // Giả sử id là string, điều chỉnh tùy theo cơ sở dữ liệu của bạn
    firstName: string;
    lastName: string;
    email: string;
    phoneNumber: string;
    birthDate: string;
    gender: string;
  }